#include "stdlib.h"

int main() {
    int x, y, z = 0;

    int a, b = 0;

    for (int i = 0; i < (5*x - z*y); i++) {
        b += z*x;
    }

    int niz1[10];
    int niz2[10];
    for (int j = 0; j < 10; j++) {
        niz1[j] = b*j;
        niz2[j] = b*x;
    }



    return 0;
}