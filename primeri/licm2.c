#include "stdlib.h"

int main() {
    int x, y, z = 0;

    int a, b = 0;

    for (int i = 0; i < (5*x - z*y); i++) {
        if (z > 2) {
            z--;
        } else {
            z += x;
        }
    }

    return 0;
}